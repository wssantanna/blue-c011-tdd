
/*

    it ou test

    - [ ] Cria o teste. Esse teste irá falhar;
        - [ ] Descrever o teste;
            - DEVE / QUANDO (Should / When)
        - [ ] Implementar o teste; 
 */

// Descrição do recurso = describe
describe('Testando as operações matemáticas da Calculadora', () => {
    
    // Descrição de uma unidade/processo do recurso = test/it
    test('DEVE somar os valores QUANDO tiver dois ou mais valores.', ()=> {
        
        expect()
            .toBe(0);
    });
    
    test('DEVE subtrair os valores QUANDO tiver dois ou mais valores.', () => {

        expect()
            .toBe(0);
    });
    
    test('DEVE deve calcular a média QUANDO tiver três.', () => {

        expect()
            .toBe(0);
    });
})