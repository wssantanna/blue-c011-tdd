
import { render, screen } from '@testing-library/react';

import App from './App';

describe('Testar o component ', () => {

	it('deve adicionar uma nova tarefa quando o formulário for disparado', async () => {
		
		// Renderizar o componente

		// Buscar o input

		// Digitar a tarefa no input

		// Buscar o botão


		// Buscar a tabela

		// Verificar se a tareefa foi adicionada na tabela
	});
})
